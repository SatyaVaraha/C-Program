#include<stdio.h>

int main()
{
    int a,i=1,c;
    printf("Enter the Number:");
    scanf("%d",&a);
    while(i<=10)
    {
    c=a*i;
    printf("\n%d * %d = %d\n",a,i,c);
    i++;
    }
    
    return 0;
}
